declare({
  schema: "raw_oltp",
  name: "orders"
})

declare({
  schema: "raw_oltp",
  name: "users"
})

declare({
  schema: "raw_oltp",
  name: "payments"
})

declare({
  schema: "raw_oltp",
  name: "escrows"
})

declare({
  schema: "raw_oltp",
  name: "user_sessions"
})

declare({
  schema: "raw_oltp",
  name: "fraud_flags"
})